meta {
  name: 03_Register_Invalid_400
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "tipo": "INVALID",
    "cuit": "123",
    "password": "123"
  }
}

tests {
  test("Status code is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
