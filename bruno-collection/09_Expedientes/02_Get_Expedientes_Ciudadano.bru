meta {
  name: 02_Get_Expedientes_Ciudadano
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/expedientes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_ciudadano}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
