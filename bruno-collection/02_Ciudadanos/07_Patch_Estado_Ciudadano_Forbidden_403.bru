meta {
  name: 07_Patch_Estado_Ciudadano_Forbidden_403
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/ciudadanos/1/estado
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_ciudadano}}
}

body:json {
  {
    "estado": "ACTIVO"
  }
}

tests {
  test("Status code is 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
