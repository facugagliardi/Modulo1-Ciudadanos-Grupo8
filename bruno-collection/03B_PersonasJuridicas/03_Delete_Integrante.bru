meta {
  name: 03_Delete_Integrante
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/personas-juridicas/2/integrantes/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_empleado}}
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
