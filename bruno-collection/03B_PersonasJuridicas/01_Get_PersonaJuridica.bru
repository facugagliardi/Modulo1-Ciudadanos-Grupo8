meta {
  name: 01_Get_PersonaJuridica
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/personas-juridicas/8
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_empleado}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
