meta {
  name: 06_Patch_Organizacion_Estado
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/organizaciones/1/estado
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_empleado}}
}

body:json {
  {
    "estado": "INACTIVO"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
