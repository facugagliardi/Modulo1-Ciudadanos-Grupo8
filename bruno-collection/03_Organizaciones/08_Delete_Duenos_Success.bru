meta {
  name: 08_Delete_Duenos_Success
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/organizaciones/1/duenos/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_ciudadano}}
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
