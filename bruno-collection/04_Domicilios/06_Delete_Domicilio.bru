meta {
  name: 06_Delete_Domicilio
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/domicilios/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_ciudadano}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
