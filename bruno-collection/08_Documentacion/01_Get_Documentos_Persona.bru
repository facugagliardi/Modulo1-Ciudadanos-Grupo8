meta {
  name: 01_Get_Documentos_Persona
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/personas/1/documentos
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_ciudadano}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
