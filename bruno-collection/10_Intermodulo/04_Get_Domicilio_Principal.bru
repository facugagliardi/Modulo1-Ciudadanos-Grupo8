meta {
  name: 04_Get_Domicilio_Principal
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/personas/1/domicilio-principal
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_empleado}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
